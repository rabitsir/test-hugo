# ==========================================
# 站点基础配置
# ==========================================

# 网站根URL（必须改为你的实际域名，末尾带斜杠）
baseURL: "https://rabitsir.icu/"

# 站点标题（显示在浏览器标签页和首页）
title: 👾 Hi, I'm Rabitsir

# 使用的主题名称（对应 themes/ 目录下的文件夹名）
theme: MyPaperMod

# 分页配置（Hugo v0.128.0+ 新格式）
# pagerSize: 每页显示的文章数量
pagination:
    pagerSize: 5  # 首页和列表页每页显示 5 篇文章

# ==========================================
# 构建控制
# ==========================================

# 允许生成 robots.txt（用于搜索引擎爬虫指引）
enableRobotsTXT: true

# 构建时包含草稿（draft: true 的文章）false=生产环境不显示草稿
buildDrafts: false

# 构建时包含未来日期的文章（发布日期在未来）false=不预发布
buildFuture: false

# 构建时包含已过期的文章（expiryDate 已过期）false=不显示过期内容
buildExpired: false

# 代码压缩配置
minify:
    disableXML: true      # 禁用 XML 文件压缩（避免某些 RSS 阅读器兼容问题）
    minifyOutput: true    # 压缩 HTML 输出（去除多余空格和换行）

# ==========================================
# 主题参数配置（PaperMod 特有）
# ==========================================

params:
    # 环境模式：production 启用 GA/OpenGraph/Twitter 卡片等 SEO 功能
    # 开发时可用 development 避免被搜索引擎索引
    env: production
    
    # 站点标题（SEO 用，若空则使用顶层的 title）
    title: ExampleSite
    
    # 站点描述（SEO 用，搜索引擎结果页显示的简介）
    description: "ExampleSite 描述"
    
    # 站点关键词（SEO 用，逗号分隔的主题词）
    keywords: [Blog, Portfolio, PaperMod]
    
    # 默认作者（显示在文章元数据，可设为字符串或列表：["Me", "You"]）
    author: Me
    
    # 默认图片（用于 OpenGraph/Twitter 卡片分享时的预览图，填绝对或相对路径）
    images: ["<用于 opengraph, twitter-cards 的图片链接或路径>"]
    
    # 日期格式（Go 语言时间格式模板，参考：https://gohugo.io/functions/time/format/）
    # "January 2, 2006" = 月 日, 年（英文格式）
    DateFormat: "January 2, 2006"
    
    # 默认主题：light（浅色）、dark（深色）、auto（跟随系统）
    defaultTheme: auto
    
    # 禁用主题切换按钮（true=隐藏右上角主题切换图标）
    disableThemeToggle: false

    # --- 文章元数据显示开关 ---
    
    # 显示阅读时间（如"5分钟阅读"）
    ShowReadingTime: true
    
    # 显示分享按钮（文章底部社交分享图标）
    ShowShareButtons: true
    
    # 显示文章导航链接（文末的上一篇/下一篇）
    ShowPostNavLinks: true
    
    # 显示面包屑导航（文章标题上方的路径：首页 > 分类 > 文章名）
    ShowBreadCrumbs: true
    
    # 显示代码复制按钮（代码块右上角复制图标）
    ShowCodeCopyButtons: false
    
    # 显示字数统计（文章元数据中的字数）
    ShowWordCount: true
    
    # 在分类/标签列表页显示 RSS 订阅按钮
    ShowRssButtonInSectionTermList: true
    
    # 使用 Hugo 原生目录而非主题自定义目录（一般保持 true）
    UseHugoToc: true
    
    # 禁用首篇文章特殊样式（true=首篇文章不放大显示）
    disableSpecial1stPost: false
    
    # 禁用返回顶部按钮（true=隐藏右下角↑按钮）
    disableScrollToTop: false
    
    # 显示评论（需在 layouts/partials/comments.html 配置具体评论系统）
    comments: false
    
    # 隐藏元数据（true=隐藏日期、阅读时间、作者、翻译链接等）
    hidemeta: false
    
    # 在列表页隐藏摘要（true=文章列表不显示摘要）
    hideSummary: false
    
    # 显示目录（Table of Contents）
    showtoc: false
    
    # 默认展开目录（true=页面加载时目录默认打开）
    tocopen: false

    # --- 资源文件配置 ---
    assets:
        # disableHLJS: true    # 禁用 highlight.js（若使用 Hugo 的 Chroma 高亮则开启）
        # disableFingerprinting: true  # 禁用 SRI（解决 integrity 校验失败问题）
        
        # 网站图标（favicon）配置，支持相对路径或绝对 URL
        favicon: "<链接 / 绝对 url>"
        favicon16x16: "<链接 / 绝对 url>"      # 浏览器标签页小图标
        favicon32x32: "<链接 / 绝对 url>"      # 高分辨率图标
        apple_touch_icon: "<链接 / 绝对 url>"  # iOS 设备添加到主屏时的图标
        safari_pinned_tab: "<链接 / 绝对 url>" # Safari 固定标签页图标（SVG）

    # --- 站点标签/Logo 配置 ---
    label:
        text: "Home"                    # 左上角导航文字（默认显示站点标题，可改）
        icon: /apple-touch-icon.png     # 标签页图标（显示在文字旁）
        iconHeight: 35                  # 图标高度（像素）

    # --- 个人资料模式（首页全屏展示）---
    # 与 homeInfoParams 二选一，enabled: true 时启用
    profileMode:
        enabled: false                  # 必须显式设为 true 才启用
        title: ExampleSite              # 大标题
        subtitle: "这是副标题"          # 副标题/签名
        imageUrl: "<图片位置>"          # 头像/主图 URL
        imageWidth: 120                 # 图片宽度（像素）
        imageHeight: 120                # 图片高度（像素）
        imageTitle: 我的图片            # 图片 alt 文本（悬停提示）
        buttons:                        # 下方按钮（可添加多个）
            - name: Posts               # 按钮文字
              url: posts                # 链接路径（相对于 baseURL）
            - name: Tags
              url: tags

    # --- 主页信息模式（首页顶部显示信息卡片）---
    # profileMode.enabled=false 时生效
    homeInfoParams:
        Title: "嗨，你好 \U0001F44B"   # 信息卡片标题（\U0001F44B 是 👋 Emoji）
        Content: 欢迎来到我的博客       # 正文内容（支持 Markdown）

    # --- 社交图标（显示在首页和资料页）---
    socialIcons:
        - name: x                       # 图标名称（对应主题内置图标）
          url: "https://x.com/"         # 链接地址
        - name: stackoverflow
          url: "https://stackoverflow.com"
        - name: github
          url: "https://github.com/"

    # --- 搜索引擎验证标签（SEO）---
    analytics:
        google:
            SiteVerificationTag: "XYZabc"  # Google Search Console 验证字符串
        bing:
            SiteVerificationTag: "XYZabc"  # Bing Webmaster Tools 验证字符串
        yandex:
            SiteVerificationTag: "XYZabc"  # Yandex 验证字符串

    # --- 封面图默认设置 ---
    cover:
        hidden: true        # 全局隐藏封面（但保留结构化数据给搜索引擎）
        hiddenInList: true  # 在文章列表页/首页隐藏封面图
        hiddenInSingle: true # 在单篇文章页隐藏封面图（若要在文章内显示设为 false）

    # --- 文章编辑链接（通常链接到 GitHub 源文件）---
    editPost:
        URL: "https://github.com/<仓库路径>/content"  # 仓库基础路径
        Text: "建议修改"                               # 按钮显示文字
        appendFilePath: true                           # 自动追加文件路径到 URL

    # --- 搜索功能配置（基于 Fuse.js）---
    fuseOpts:
        isCaseSensitive: false      # 区分大小写搜索（false=不区分）
        shouldSort: true            # 按匹配度排序结果
        location: 0                 # 匹配起始位置（0=从头开始）
        distance: 1000              # 匹配距离阈值（越大容错越高）
        threshold: 0.4              # 匹配阈值（0=精确，1=宽松）
        minMatchCharLength: 0       # 最小匹配字符数（0=任意长度）
        limit: 10                   # 最大返回结果数（参考 Fuse.js 文档）
        keys: ["title", "permalink", "summary", "content"]  # 搜索字段

# ==========================================
# 导航菜单配置
# ==========================================

menu:
    main:
        # 分类菜单项
        - identifier: categories    # 唯一标识符（不能重复）
          name: categories          # 显示文字（可改中文"分类"）
          url: /categories/         # 链接路径（相对于 baseURL）
          weight: 10                # 排序权重（越小越靠左）
        
        # 标签菜单项  
        - identifier: tags
          name: tags                # 可改为"标签"
          url: /tags/
          weight: 20
        
        # 外部链接示例
        - identifier: example
          name: example.org
          url: https://example.org  # 外部 URL（自动识别并添加外部图标）
          weight: 30

# ==========================================
# 代码语法高亮配置
# ==========================================

# 使用 CSS 类名高亮（而非内联样式），配合自定义 CSS 主题
pygmentsUseClasses: true

markup:
    highlight:
        noClasses: false            # false=使用 CSS 类（需配合上面 pygmentsUseClasses）
        # anchorLineNos: true      # 行号添加锚点链接
        # codeFences: true         # 启用代码围栏（``` 语法）
        # guessSyntax: true        # 自动猜测语言（无指定时）
        # lineNos: true            # 显示行号
        # style: monokai           # 内置主题样式（若 noClasses=true 时生效）